<h2>グループ一覧</h2>

<%= link_to "グループ作成", new_group_path %>

<% @groups.each do |group| %>
  <div>
    <h3><%= link_to group.name, group_path(group) %></h3>
    <p><%= group.introduction.truncate(100) %></p>
    <p>（メンバー数：<%= group.users.count %>）</p>
  </div>
<% end %>